# Build artifacts — regenerate with ./build/zip.sh
dist/

# Progress files, if a course is ever run from inside this repo
.ai-docent/

# OS
.DS_Store

# Local Python env for running build/validate.py
.venv/
venv/
